"""LangChain adapters and LCEL orchestration for the ExploreRAG pipeline.

The package deliberately depends on the existing domain services rather than
replacing their optimized document parsing, GPU scheduling, vector storage or
knowledge-graph implementations.
"""

